import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#c3a149",
      light: "skyblue",
    },
    secondary: {
      main: "#c3a149",
    },
    otherColor: {
      main: "#c3a149",
    },
  },
});
